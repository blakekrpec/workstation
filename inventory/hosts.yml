---
# Single-host inventory: this machine, managed locally.
# Per-machine overrides live in host_vars/<hostname>.yml, keyed on the
# inventory_hostname below rather than the real hostname.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
